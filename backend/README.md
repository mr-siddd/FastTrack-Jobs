# FastTrack Backend

This folder contains a TypeScript Fastify backend that orchestrates AI and Playwright MCP interactions.

Quick start (Windows PowerShell):

```powershell
cd backend
npm install
npm run dev
```

Notes:
- Endpoints are stubbed to allow UI integration and iterative development.
- Configure `OPENAI_API_KEY` and other env vars via `.env` when ready.
