# Playwright MCP Server

This folder is a placeholder for Playwright MCP server wrapper and run instructions. The plan is to run a local MCP-compatible server (headed Chromium) that the backend will call.

For development, you can start a Playwright script that listens for commands, or run Playwright directly from the backend.
